{
  "name": "finovert-project",
  "private": true,
  "description": "Finovert Monorepo (Frontend + Backend)",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "server": "npm run dev --workspace=backend",
    "start": "npm start --workspace=backend",
    "install-all": "npm install"
  }
}
